from setuptools import setup

setup(
   name='django-admin-magnify',
   version='0.0',
   description='A useful module',
   author='Louis Grange',
   packages=['django-admin-magnify'],
   install_requires=['django>=4.0'],
)
